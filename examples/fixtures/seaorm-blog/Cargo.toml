[package]
name = "openmodels-seaorm-blog"
version = "0.1.0"
edition = "2021"

[dependencies]
sea-orm = { version = "1.1.10", default-features = false, features = ["macros", "with-chrono", "with-uuid"] }
